# Allow all crawlers to index the public site.
# Dev / API routes are blocked from indexing — they don't make sense as
# search results (auth-gated UI, JSON endpoints).
User-agent: *
Allow: /
Disallow: /dev/
Disallow: /api/
Disallow: /auth/
Disallow: /profile/
Disallow: /settings/
Disallow: /onboarding/
