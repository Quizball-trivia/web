"use client";

/** Dev-only preview of the /mini-games hub — no auth needed. */

import MiniGamesPage from "@/app/(app)/mini-games/page";

export default function MiniGamesHubPreview() {
  return <MiniGamesPage />;
}
